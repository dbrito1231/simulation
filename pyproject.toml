[project]
name = "simulation"
version = "0.1.0"
description = "Browser-based AI village simulation powered by a local LLM"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "flask>=3.1.3",
    "flask-cors>=6.0.5",
    "requests>=2.34.2",
]
